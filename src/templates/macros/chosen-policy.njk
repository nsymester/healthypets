{% macro data(info) %}
{% if info.cover == 'lifetime' %}
  {% set cover = 'Lifetime'%}
{%elif info.cover == 'maximum' %}
  {% set cover = 'Max Ben'%}
{%elif info.cover == 'accident' %}
  {% set cover = 'Accident'%}
{% endif %}
    <section class="policy-summary {{ info.marginBottom }} ">
      <div class="card pet-summary mb-0 rounded-0 bg-white {{ info.brdColour }}">
        <p class="policy-summary__complete-icon"><span><i class="fas fa-check"></i></span></p>
        <h5 class="pet-summary__header">{{info.name}}, {{info.breed}}, {{info.age}}</h5>
        {% if info.state == 'form' %}
        <div class="pet-summary__items">
          <a class="pet-summary__link" href="">Edit Bobby's Info</a>
          <a class="pet-summary__link" href="">Delete</a>
        </div>
        {% endif %}
      </div>
      <div class="card mt-0 rounded-0">
        <div class="policy-summary__offer">
          <h5>{{cover}} {{info.cost}}</h5>
          <p class="policy-summary__price">£29.11<small>/month</small></p>
        </div>
        {% if info.state == 'form' %}
        <a href="">Edit cover</a>
        {% endif %}
      </div>
    </section>
    {% if info.state == 'paid' %}
      <p><a href="">Download Policy documents</a></p>
    {% endif %}
{% endmacro %}